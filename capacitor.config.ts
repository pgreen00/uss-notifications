import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.unitedsystems.notifications',
  appName: 'Notifications',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
